const MENU=[{"id": 1, "name": "House Flat White", "cat": "coffee", "price": 190, "img": "assets/coffee.svg", "desc": "Double espresso, silky milk, signature roast."}, {"id": 2, "name": "Iced Caramel Latte", "cat": "coffee", "price": 230, "img": "assets/iced.svg", "desc": "Cold espresso, milk, caramel, and ice."}, {"id": 3, "name": "Avocado Toast", "cat": "food", "price": 320, "img": "assets/avocado.svg", "desc": "Sourdough, avocado, feta, seeds, and herbs."}, {"id": 4, "name": "Breakfast Croissant", "cat": "food", "price": 280, "img": "assets/croissant.svg", "desc": "Flaky croissant with egg, cheese, and greens."}, {"id": 5, "name": "Chocolate Cloud", "cat": "sweet", "price": 210, "img": "assets/cake.svg", "desc": "Dark chocolate cake with whipped cream."}, {"id": 6, "name": "Cinnamon Roll", "cat": "sweet", "price": 170, "img": "assets/roll.svg", "desc": "Warm roll, cinnamon sugar, vanilla glaze."}];const CART='bbCart';
const getCart=()=>{try{return JSON.parse(localStorage.getItem(CART)||'{}')}catch{return {}}};function saveCart(c){localStorage.setItem(CART,JSON.stringify(c));updateBadge()}function updateBadge(){let n=Object.values(getCart()).reduce((a,b)=>a+b,0);document.querySelectorAll('.cart-count').forEach(x=>x.textContent=n)}
function toast(t){let e=document.querySelector('.toast');if(!e){e=document.createElement('div');e.className='toast';document.body.appendChild(e)}e.textContent=t;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),1600)}function addToCart(id){let c=getCart();c[id]=(c[id]||0)+1;saveCart(c);toast('Added to your order')}
function renderMenu(f='all'){let g=document.getElementById('menuGrid');if(!g)return;g.innerHTML=MENU.filter(x=>f==='all'||x.cat===f).map(x=>`<article class="menu-card"><img src="${x.img}" alt="${x.name}"><div class="menu-body"><div class="row"><h3>${x.name}</h3><span class="price">₹${x.price}</span></div><p>${x.desc}</p><button class="add" onclick="addToCart(${x.id})">Add to order</button></div></article>`).join('')}
function renderCart(){let box=document.getElementById('cartItems');if(!box)return;let c=getCart(),es=Object.entries(c);box.innerHTML=es.length?es.map(([id,q])=>{let x=MENU.find(m=>m.id==id);return `<div class="cart-item"><img src="${x.img}"><div><h3>${x.name}</h3><span class="price">₹${x.price}</span></div><div><div class="qty"><button onclick="qty(${id},-1)">−</button><b>${q}</b><button onclick="qty(${id},1)">+</button></div><button class="remove" onclick="removeItem(${id})">Remove</button></div></div>`}).join(''):'<div class="empty"><h2>Your cart is empty</h2><p>Choose something delicious.</p><a class="btn" href="menu.html">Browse menu</a></div>';let sub=es.reduce((s,[id,q])=>s+MENU.find(x=>x.id==id).price*q,0);['subtotal','tax','grand'].forEach(id=>{let e=document.getElementById(id);if(e)e.textContent='₹'+(id==='subtotal'?sub:id==='tax'?Math.round(sub*.05):sub+Math.round(sub*.05))})}
function qty(id,n){let c=getCart();c[id]+=n;if(c[id]<=0)delete c[id];saveCart(c);renderCart()}function removeItem(id){let c=getCart();delete c[id];saveCart(c);renderCart()}function checkout(){if(!Object.keys(getCart()).length)return toast('Your cart is empty');localStorage.removeItem(CART);updateBadge();renderCart();document.getElementById('orderSuccess').classList.add('show')}
document.addEventListener('DOMContentLoaded',()=>{updateBadge();renderMenu();renderCart();document.body.classList.add('loaded');document.querySelectorAll('.filter').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');renderMenu(b.dataset.filter)});document.querySelector('.menu-toggle')?.addEventListener('click',()=>document.querySelector('.mobile-menu').classList.toggle('open'));let io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('seen')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e))});
