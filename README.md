# Morrow Cafe

GitHub Pages-ready multi-page website. Upload the contents of this folder to the repository root. Keep the lowercase `assets` folder beside the HTML files. Images are included PNG files and use `./assets/...` relative paths.
